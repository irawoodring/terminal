# Terminal

A capture-the-flag style game for people who want to get better at using bash. Runs on WebVM, completely in a player's browser. No worries about messing anything up; just reload the page to get back to the starting state. No networking layer is attached.  As this is a proof-of-concept, code is a fork of the original project instead of starting from scratch.
